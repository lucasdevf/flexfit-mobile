import styled from 'styled-components/native'

import { SafeAreaView } from 'react-native-safe-area-context'

export const TrainingContainer = styled(SafeAreaView)`
  padding: 24px;
`

export const Options = styled.View`
  margin-top: 24px;
`
